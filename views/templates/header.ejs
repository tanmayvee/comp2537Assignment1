<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>COMP 2537 Assignment 2</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f4f4f4; }
        .navbar { background-color: #1a5c2a; }
        .navbar-brand, .nav-link { color: #c9ab20 !important; font-weight: 600; }
        .nav-link:hover { color: #ffffff !important; }
        .btn-primary { background-color: #1a5c2a; border-color: #1a5c2a; }
        .btn-primary:hover { background-color: #144a22; border-color: #144a22; }
        .btn-gold { background-color: #c9ab20; border-color: #c9ab20; color: #000; font-weight: 600; }
        .btn-gold:hover { background-color: #a88f1a; border-color: #a88f1a; color: #000; }
        .btn-outline-primary { color: #1a5c2a; border-color: #1a5c2a; }
        .btn-outline-primary:hover { background-color: #1a5c2a; color: #fff; }
        .btn-success { background-color: #1a5c2a; border-color: #1a5c2a; }
        .btn-warning { background-color: #c9ab20; border-color: #c9ab20; color: #000; }
        footer { background-color: #111111; }
        thead.table-dark th { background-color: #111111; color: #c9ab20; }
        .badge { background-color: #c9ab20 !important; color: #000 !important; font-weight: 600; }
        h1, h2, h4 { color: #1a5c2a; }
        .display-1 { color: #111111; }
        .card { border: 2px solid #1a5c2a; }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/">MyApp</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon" style="filter: invert(1);"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                <li class="nav-item"><a class="nav-link" href="/members">Members</a></li>
                <li class="nav-item"><a class="nav-link" href="/admin">Admin</a></li>
                <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
            </ul>
        </div>
    </div>
</nav>
<div class="container mt-4">
